http {
    listen $PORT default_server;

    location /static {
        alias /vol/static;
    }

    location /logs {
        error_log /var/log/nginx/logs.error.log debug;
    }

    location / {
        proxy_pass netmanga.herokuapp.com:$PORT;
        include /etc/nginx/uwsgi_params;
        
    }
}
